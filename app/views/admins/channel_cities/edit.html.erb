<div class="row">
  <%= breadcrumb do %>
    <%= breadcrumb_item %>
    <%= breadcrumb_item 'Channel Cities', admins_channel_cities_path %>
    <%= breadcrumb_item 'Edit Channel City' %>
  <% end %>
</div>

<div class="row">
  <div class="col-lg-12">
    <h1 class="page-header">Edit <%= @channel_city.city %></h1>
  </div>
</div>

<%= render 'form', channel_city: @channel_city %>
