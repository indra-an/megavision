<div class="row">
  <div class="col-lg-12">
    <div class="panel panel-default">
      <div class="panel-body">
        <%= form_for([:admins, channel_city], :html => { :class => 'form-horizontal' }) do |f| %>
          <% if channel_city.errors.any? %>
            <div class="alert alert-danger" role="alert">
              <strong>Ops! <%= pluralize(channel_city.errors.count, "error") %> found:</strong><br/>
              <ul>
                <% channel_city.errors.full_messages.each do |message| %>
                  <li><%= message %></li>
                <% end %>
              </ul>
            </div>
          <% end %>

          <div class="form-group">
            <%= f.label :city, :class => 'col-sm-2 control-label' %>
            <div class="col-sm-10">
              <%= f.text_field :city, :class => 'form-control' %>
            </div>
          </div>

           <div class="form-group">
            <%= f.label :channels, :class => 'col-sm-2 control-label' %>
            <div class="col-sm-10">
              <%= f.select :channel_ids, @channels, {}, :multiple => true, :class => 'chosen' %>
            </div>
          </div>

          <div class="row">
            <div class="col-md-6 text-right">
              <%= link_to admins_channel_cities_path, :class => 'btn btn-warning' do %>
                <span class="glyphicon glyphicon-menu-left"></span>&nbsp;Back
              <% end %>
            </div>
            <div class="col-md-6 text-left">
              <%= f.submit 'Save', :class => 'btn btn-primary' %>
            </div>
          </div>
        <% end %>
      </div>
    </div>
  </div>
</div>
