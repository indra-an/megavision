<div class="row">
  <div class="col-lg-12">
    <div class="panel panel-default">
      <div class="panel-body">
        <%= form_for([:admins, area_coverage], :html => { :class => 'form-horizontal' }) do |f| %>
          <% if area_coverage.errors.any? %>
            <div class="alert alert-danger" role="alert">
              <strong>Ops! <%= pluralize(area_coverage.errors.count, "error") %> found:</strong><br/>
              <ul>
                <% area_coverage.errors.full_messages.each do |message| %>
                  <li><%= message %></li>
                <% end %>
              </ul>
            </div>
          <% end %>

          <div class="form-group">
            <%= f.label :city, :class => 'col-sm-2 control-label' %>
            <div class="col-sm-10">
              <%= f.select :channel_city_id, @channel_cities, {}, :multiple => false, :class => 'chosen' %>
            </div>
          </div>

          <div class="form-group">
            <%= f.label :code, :class => 'col-sm-2 control-label' %>
            <div class="col-sm-10">
              <%= f.select :area_code_id, @area_codes, {}, :multiple => false, :class => 'chosen' %>
            </div>
          </div>

          <div class="form-group">
            <%= f.label :area, :class => 'col-sm-2 control-label' %>
            <div class="col-sm-10">
              <%= f.text_field :area, :class => 'form-control' %>
            </div>
          </div>

          <div class="row">
            <div class="col-md-6 text-right">
              <%= link_to admins_area_coverages_path, :class => 'btn btn-warning' do %>
                <span class="glyphicon glyphicon-menu-left"></span>&nbsp;Back
              <% end %>
            </div>
            <div class="col-md-6 text-left">
              <%= f.submit 'Save', :class => 'btn btn-primary' %>
            </div>
          </div>
        <% end %>
      </div>
    </div>
  </div>
</div>
