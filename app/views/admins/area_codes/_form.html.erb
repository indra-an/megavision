<div class="row">
  <div class="col-lg-12">
    <div class="panel panel-default">
      <div class="panel-body">
        <%= form_for([:admins, area_code], :html => { :class => 'form-horizontal' }) do |f| %>
          <% if area_code.errors.any? %>
            <div class="alert alert-danger" role="alert">
              <strong>Ops! <%= pluralize(area_code.errors.count, "error") %> found:</strong><br/>
              <ul>
                <% area_code.errors.full_messages.each do |message| %>
                  <li><%= message %></li>
                <% end %>
              </ul>
            </div>
          <% end %>

          <div class="form-group">
            <%= f.label :code, :class => 'col-sm-2 control-label' %>
            <div class="col-sm-10">
              <%= f.text_field :code, :class => 'form-control' %>
            </div>
          </div>

           <div class="form-group">
            <%= f.label :type, :class => 'col-sm-2 control-label' %>
            <div class="col-sm-10">
              <%= f.select :channel_type_ids, @channel_types, {}, :multiple => false, :class => 'chosen' %>
            </div>
          </div>

          <div class="row">
            <div class="col-md-6 text-right">
              <%= link_to admins_area_codes_path, :class => 'btn btn-warning' do %>
                <span class="glyphicon glyphicon-menu-left"></span>&nbsp;Back
              <% end %>
            </div>
            <div class="col-md-6 text-left">
              <%= f.submit 'Save', :class => 'btn btn-primary' %>
            </div>
          </div>
        <% end %>
      </div>
    </div>
  </div>
</div>
