<div class="row">
  <%= breadcrumb do %>
    <%= breadcrumb_item %>
    <%= breadcrumb_item 'Channel Package', admins_channel_city_type_channel_packages_path(params[:channel_city_id], params[:type_id]) %>
    <%= breadcrumb_item 'Edit Channel Package' %>
  <% end %>
</div>

<div class="row">
  <div class="col-lg-12">
    <h1 class="page-header">Edit <%= @channel_package.package.name %></h1>
  </div>
</div>

<%= render 'form', channel_package: @channel_package %>
