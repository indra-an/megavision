<div class="row">
  <%= breadcrumb do %>
    <%= breadcrumb_item %>
    <%= breadcrumb_item 'Landing Images', admins_landing_images_path %>
    <%= breadcrumb_item 'Edit Landing Image' %>
  <% end %>
</div>

<div class="row">
  <div class="col-lg-12">
    <h1 class="page-header">Edit <%= @landing_image.title %></h1>
  </div>
</div>

<%= render 'form', landing_image: @landing_image %>

