<div class="row">
  <div class="col-lg-12">
    <div class="panel panel-default">
      <div class="panel-body">
        <%= form_for([:admins, price], :html => { :class => 'form-horizontal' }) do |f| %>
          <% if price.errors.any? %>
            <div class="alert alert-danger" role="alert">
              <strong>Ops! <%= pluralize(price.errors.count, "error") %> found:</strong><br/>
              <ul>
                <% price.errors.full_messages.each do |message| %>
                  <li><%= message %></li>
                <% end %>
              </ul>
            </div>
          <% end %>

          <div class="form-group">
            <%= f.label :amount, :class => 'col-sm-2 control-label' %>
            <div class="col-sm-10">
              <%= f.text_field :amount, :class => 'form-control' %>
            </div>
          </div>

          <div class="form-group">
            <%= f.label :up_to_speed, :class => 'col-sm-2 control-label' %>
            <div class="col-sm-10">
              <%= f.text_field :up_to_speed, :class => 'form-control' %>
            </div>
          </div>

          <div class="form-group">
            <%= f.label :unit, :class => 'col-sm-2 control-label' %>
            <div class="col-sm-10">
              <%= f.select :unit, [['Kbps', 'Kbps'], ['Mbps', 'Mbps']], {}, :class => 'form-control' %>
            </div>
          </div>

          <div class="row">
            <div class="col-md-6 text-right">
              <%= link_to admins_prices_path, :class => 'btn btn-warning' do %>
                <span class="glyphicon glyphicon-menu-left"></span>&nbsp;Back
              <% end %>
            </div>
            <div class="col-md-6 text-left">
              <%= f.submit 'Save', :class => 'btn btn-primary' %>
            </div>
          </div>
        <% end %>
      </div>
    </div>
  </div>
</div>
